%h4
  Tytuły dostarczane regularnie:
- @titles.each  do |frequency, ot|
  %h5=frequency
  - count = 1
  - ot.each do |title|
    %p
      ="#{count}. "
      = title.title.title
      %span{style: 'margin-left:50px;'}
        = title.quantity
      - count = count + 1

