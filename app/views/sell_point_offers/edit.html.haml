%h3 
  Offer for 
  =@sell_point.name

%div{style:'margin-top:20px;'}
  = form_for @offer, url: sell_point_offer_path(id: @offer.id, sell_point_id: @sell_point.id) do |f|
    = render 'form', f: f

= link_to 'Back', {action: :index}
